def send_proposal(email, email_password, email_imap, socks_proxy, http_proxy, first_name, last_name, fifa_password,
                  headless=True) -> bool:
    # в случае успешной регистрации возвращаем True
    # в случае любых проблем с регистрацией - возвращаем Exception, в тексте Exception должна быть информация,
    # по которой легко определить причину ошибки
    raise Exception('Not implemented')
